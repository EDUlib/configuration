- name: Configure instance(s)
  hosts: all
  sudo: True
  gather_facts: True
  vars:
    migrate_db: 'yes'
    openid_workaround: true
    EDXAPP_LMS_NGINX_PORT: '80'
    EDX_ANSIBLE_DUMP_VARS: true
    CERTS_DOWNLOAD_URL: 'https://edulib-certificats.s3.amazonaws.com'
    CERTS_VERIFY_URL: 'http://ec2-52-2-47-62.compute-1.amazonaws.com:18090'
    # used for releases
    edx_platform_version: 'edulib-dogwood-rc'
    certs_version: 'edulib-dogwood-rc'
    forum_version: 'edulib-dogwood-rc'
    xqueue_version: 'edulib-dogwood-rc'
    NOTIFIER_VERSION: 'edulib-dogwood-rc'
  roles:
    - common
    - vhost
    - edx_ansible
    - user
    - role: nginx
      nginx_sites:
      - cms
      - lms
      - forum
      - xqueue
      - certs
      nginx_default_sites:
      - lms
      - cms
    - mysql
    - edxlocal
    - mongo
    - edxapp
    - { role: 'edxapp', celery_worker: True }
    - notifier
    - demo
    - { role: 'rabbitmq', rabbitmq_ip: '127.0.0.1' }
    - oraclejdk
    - elasticsearch
    - forum
    - { role: "xqueue", update_users: True }
    - certs
    - role: analytics_api
      when: ANALYTICS_API_GIT_IDENTITY
